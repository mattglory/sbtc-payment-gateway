
{
  "name": "sbtc-gateway-tests",
  "version": "1.0.0",
  "description": "Run unit tests on this project.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:report": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hirosystems/clarinet-sdk": "^1.8.0",
    "@stacks/transactions": "^6.16.0", 
    "chokidar-cli": "^3.0.0",
    "vitest": "^1.6.0",
    "vitest-environment-clarinet": "^1.1.0"
  },
  "engines": {
    "node": ">=20.18.1 <=22.12.0",
    "npm": ">=9.0.0"
  }
}
